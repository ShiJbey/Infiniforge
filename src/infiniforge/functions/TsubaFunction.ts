/**
 * Generates a katana blade guard known as a Tsuba.
 * Tsuba are placed above the handle to protect the
 * wielder's hands.
 */
export default class TsubaFunction {
  /** Width of the blade from cutting edge to spine (m) */
  public bladeWidth = 0.04;
  /** Thickness of the generated tsuba (m) */
  public thickness = 0.01;
}
