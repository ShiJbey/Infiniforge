/** Hashes the seed value for passing to python */
function hashSeed( seed ) { return seed; };

/** Saves the current recipe in JSON format */
function exportRecipeJSON( ) { };

/** Sends the render request by forking and starting the python script */
function renderSword( ) {
    console.log( 'Starting Blender...' );
    console.log( 'Rendering...' );
    console.log( 'Loading mesh...' );
    console.log( 'Done' );
 };